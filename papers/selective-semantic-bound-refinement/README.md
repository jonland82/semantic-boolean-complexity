# Can Semantic Learning Tighten a Mathematical Bound?

This four-page research note reports the frozen prospective cost-14 test of
selective semantic residual-slack refinement. It connects the earlier semantic
feature and learned-envelope results to the new construction-bound experiment,
states the refinement formally, and treats the failed 99% coverage criterion
as the central scientific result.

Compile from this directory:

```powershell
latexmk -pdf selective-semantic-bound-refinement.tex
```

The numerical source is:

```text
experiments/selective_slack_refinement/
```
